# Need to import all of the tkinter module
from tkinter import *
from turtle import back

# Create the actual GUI as a variable
gui = Tk()

# Set a background color
gui.configure(background="white")

# Set GUI size
gui.geometry("500x500")

# Launch the GUI window
gui.mainloop()
